# Phase 21 verification

CI must run the repository's standard API workflow. Unit coverage validates path containment, operation budgets, payload bounds, approval gates, compatibility exports, conservative planning, and deterministic stop-on-failure behavior. Production provider execution remains adapter-scoped.