# Multilingual Release Gate

The multilingual assistant release requires API tests, database migration verification, and Android CI. Language detection and response locale never authorize device actions; Safety Kernel and the authenticated command bridge remain authoritative.
