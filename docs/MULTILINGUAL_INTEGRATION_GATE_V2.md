# Multilingual Integration Gate V2

API and Android multilingual integration must pass API contracts and Android build CI before merge. Language selection and detection must never grant execution authority. PostgreSQL persistence and owner isolation must remain intact.
