# Android service boundary

The authenticated Android command service is non-exported and bounded by the capability registry. Runtime physical-device verification remains required before claiming device control.
