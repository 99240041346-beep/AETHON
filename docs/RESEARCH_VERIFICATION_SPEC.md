# Research Verification

`claims + source evidence -> supported | disputed | unsupported + provenance`.

The verifier is deterministic and bounded, accepts only HTTP(S) evidence, preserves source identifiers, and never treats research content as executable instructions. Disputed and unsupported claims remain explicit and cannot silently become verified results. Actions derived from research must still pass AgentBrain and SafetyKernel controls.
